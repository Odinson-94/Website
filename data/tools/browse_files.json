{
  "name": "browse_files",
  "desc": "List files and folders in a directory. Returns names, sizes, and types. Use to explore the user's project files. Restricted to allowed directories.",
  "bridge": "",
  "category": "context",
  "always": false,
  "param_count": 3,
  "parameters": [
    {
      "name": "path",
      "type": "string",
      "description": "Directory path to list. Omit to show allowed root directories.",
      "required": false
    },
    {
      "name": "depth",
      "type": "string",
      "description": "How deep to recurse. Default: 1. Max: 3.",
      "required": false
    },
    {
      "name": "pattern",
      "type": "string",
      "description": "Optional glob filter, e.g. '*.csv' or '*.txt'",
      "required": false
    }
  ],
  "keywords": [
    "browse files",
    "list files",
    "directory",
    "folder contents",
    "file system"
  ]
}